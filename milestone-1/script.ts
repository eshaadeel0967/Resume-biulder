// Select the button and the info section
const toggleInfo = document.getElementById('toggle-info') as HTMLButtonElement;
const infoSection = document.getElementById('personal-info') as HTMLElement;

// Add a click event listener to the button
toggleInfo.addEventListener('click', () => {
    // Toggle the display of the info section
    if (skillsSection.style.display === 'none') {
        skillsSection.style.display = 'block';
    } else {
        skillsSection.style.display = 'none';
    }
});
// Select the button and the  section
const toggleEducation = document.getElementById('toggle-skills') as HTMLButtonElement;
const eduSection = document.getElementById('skills') as HTMLElement;

// Add a click event listener to the button
toggleEducation.addEventListener('click', () => {
    // Toggle the display of the Education section
    if (skillsSection.style.display === 'none') {
        skillsSection.style.display = 'block';
    } else {
        skillsSection.style.display = 'none';
    }
});
// Select the button and the skills section
const toggleButton = document.getElementById('toggle-skills') as HTMLButtonElement;
const skillsSection = document.getElementById('skills') as HTMLElement;

// Add a click event listener to the button
toggleButton.addEventListener('click', () => {
    // Toggle the display of the skills section
    if (skillsSection.style.display === 'none') {
        skillsSection.style.display = 'block';
    } else {
        skillsSection.style.display = 'none';
    }
});

// Select the button and the skills section
const toggleButton = document.getElementById('toggle-skills') as HTMLButtonElement;
const skillsSection = document.getElementById('skills') as HTMLElement;

// Add a click event listener to the button
toggleButton.addEventListener('click', () => {
    // Toggle the display of the skills section
    if (skillsSection.style.display === 'none') {
        skillsSection.style.display = 'block';
    } else {
        skillsSection.style.display = 'none';
    }
});
